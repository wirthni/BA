\documentclass[
  a4paper,
  12pt,
  oneside,
  parskip=half,
  listof=totoc,
  bibliography=totoc,
  DIV=12,
  numbers=noenddot,
  BA.tex
  ]{subfiles}

\begin{document}

\section{Analysis of heavy ion collision data}
\label{sec:analysis-of-heavy-ion-collision-data}

\subsection{Anisotropic flow in heavy ion collisions}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\linewidth]{Media/Pictures/EllipticFlow}
	\caption{Schematic illustrations of a $\sqrt{S_{NN}} = 200\bunit{GeV}$ Au--Au collision with a $6\bunit{fm}$ impact parameter. The left panel shows the nucleons of the two colliding nuclei with an ellipse outlining the approximate interaction region. The right panel shows a momentum-space representation of $v_2$. The average radius of each successive ring represents the $p_T$ of the particles while the anisotropy of the ring represents the magnitude of $v_2$. The highest $p_T$ particles (outer-ring) exhibit the strongest $v_2$ while the lowest $p_T$ particles (inner-ring) exhibit a vanishingly small $v_2$. From \cite{collaboration_centrality_2021}}
	\label{fig:ellipticflow}
\end{figure}

In the first chapter we have talked about the phenomenologies of heavy ion collisions at collider energies in the case where the cross sections of the two nuclei fully overlap. What has not been talked about yet is what happens when two nuclei collide non-centrally. The centrality of the collision of two nuclei is characterized by the impact parameter $|\vec{b}|$ between the nuclei, so the distance between their centers in the transverse direction. \cite{collaboration_centrality_2021} The smaller the impact parameter is the bigger the overlap region of the nuclei is, thus increasing the size of the QGP medium. The centrality of events is categorized from 0-100\%, where low values describe the most central events, e.g. 0-10\%. The transverse impact vector $\vec{b}$ and the $z$-axis (beam direction) span the reaction plane with azimuthal angle $\psi$. In figure \ref{fig:ellipticflow} the reaction plane lies in the $x-z$ plane and its azimuthal angle is $\psi = 0$. The reaction plane angle is generally unknown, but it is possible to estimate it using the azimuthal distribution of particle momenta. In a hydrodynamic approach the initial spatial anisotropy of the QGP, that is the almond shape of the overlap region between non-centrally colliding nuclei, and subsequent interactions among the constituents result in pressure gradients that are larger in the direction of the reaction plane compared to out of this plane. This results in an in-plane enhancement of particle momenta (see figure \ref{fig:ellipticflow}). We call the estimation of the reaction plane angle by particle momenta the event plane angle $\psi_m$. A Fourier decomposition is used to quantify this eccentricity and other effects that lead to anisotropies in azimuthal particle momenta: \cite{voloshin_flow_1996}

\begin{equation}
	\frac{d^3 N}{p_T d p_T dy d(\phi - \psi)} = \frac{1}{2 \pi} \frac{d N}{p_T d p_T dy} \cdot \left[1 + 2 \sum_{n=1}^{\infty} v_n \cos(n(\phi - \psi_m)) \right] \, .
\end{equation}

$\phi$ is the azimuth angle, $\psi_m$ is the event plane angle, $y$ is the longitudinal rapidity and $p_T$ is the transverse momentum. The coefficients $v_n$ quantify the strength of the $n$-th order harmonic in azimuthal particle correlation relative to the event plane.

The first order harmonic $v_1 \approx 10^{-3}$ \cite{selyuzhenkov_charged_2011} is called directed flow. It describes an enhancement of particle momenta in one azimuthal direction while suppressing them in the opposite direction. \cite{voloshin_flow_1996} The directed flow can be categorized into rapidity-even ($v_1^\text{even}(\eta) = v_1^\text{even}(- \eta)$) and rapidity-odd ($v_1^\text{odd}(\eta) = - v_1^\text{odd}(- \eta)$) components. Rapidity-even directed flow comes from event-wise fluctuations \cite{luzum_directed_2011} and rapidity-odd flow originates in hydrodynamic effects similar to those of elliptic flow. The spatial medium configuration that leads to directed flow is pictured in figure \ref{fig:directedflowsketch}.


\begin{figure}
	\centering
	\includegraphics[width=0.7\linewidth]{Media/Pictures/directed_flow_sketch}
	\caption{The medium configuration shortly after a heavy ion collision. Non-uniform pressure gradients will enhance particle momenta in the positive $x$ direction for negative pseudorapidities and in the negative $x$ direction for positive pseudorapidities. From \cite{collaboration_directed_2013}.}
	\label{fig:directedflowsketch}
\end{figure}


The second order harmonic $v_2 \approx 10^{-1}$ \cite{adamczyk_elliptic_2013} is called the elliptic flow \cite{voloshin_flow_1996} and has its physical origin mainly in the initially almond-shaped QGP and the subsequent different pressure gradients in and out of the event plane.

The third order harmonic $v_3 \approx 10^{-1}$, which is called triangular flow, and also higher harmonics describe event-wise initial geometry fluctuations. \cite{alver_collision_2010}

Since the diffusion wake measurement relies on very tiny particle correlation differences it is sensitive to these kinds of anisotropic flow. Since the parameters $v_2, v_3$ have a significantly greater magnitude than $v_1$ we will take special care of the elliptic and the triangular flow whenever the analysis requires it. 

\subsection{Properties of events and particles}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\linewidth]{Media/Pictures/ALICE_COORDINATES}
	\caption{The coordinate system utilized at ALICE. The x-axis points into the direction of the accelerator’s center and the y-axis points upwards. The z-axis points in such direction that the three axes form a right-handed coordinate system. Image taken from \cite{coordination_alice_technical_definition_2003}}
	\label{fig:alicecoordinates}
\end{figure}

When analyzing heavy ion collision data from ALICE one has to know first which quantities, may they be event-related or track-related, are important to be taken care of for each specific analysis. For serving the purpose of this thesis, we will therefore only take into account those quantities that are or could be needed to conduct an heavy ion analysis regarding the diffusion wake. Let us first look at interesting event-specific properties. The position of the primary vertex $(x,y,z)$ describes how far the event has happened away form the nominal interaction point and will be needed for only selecting events that have a low $z$ value. We do this because data taking at ALICE is only calibrated for a certain range around the nominal interaction point. As another important event property the multiplicity $N$ of an event will hold the number of charged particle tracks in the corresponding event. Since we will be taking particle correlation differences between different events, it may become important to do so between similarly populated events.

Particles, or tracks, will have individual properties measured by a combination of the ALICE detector systems, including the particle charge $q$, the initial relativistic momentum $\vec{p} = (P^1, P^2, P^3) = (p_x, p_y, p_z)$, the particle dependent TPC energy loss $dE/dx$ for possible particle identification, and the TPC track length. We will remove particles that have their reconstructed trajectories far away from the primary vertex position to avoid the analysis of particles produced in interactions with the detector structure. This is characterized by the distance to closest approach (DCA) values DCA$_z$ and DCA$_{xy}$.

From these particle properties one can deduce further useful quantities. Since most of the particles created in Pb--Pb collisions at ALICE are pions \cite{ortiz_results_2014}, we will be assuming $m = m(\pi^0)$. Because of this assumption the relativistic energy-mass-momentum relation simplifies to $P^0 = E = \sqrt{m_\pi^2 + |\vec{p}|^2}$. According to the ALICE coordinate system (figure \ref{fig:alicecoordinates}), the azimuthal angle can be acquired by $\phi = \atan(p_y, p_x)$ and the transverse momentum is $p_T = \sqrt{p_x^2 + p_y^2}$. Most of the data analysis will take place in the $(\phi, \eta, p_T)$ phase space or a subset of it and to get an idea of how data will look like in this case, figure \ref{fig:dijet2dexample} shows an exemplary event three-dimensionally. The $(\phi, \eta)$ plane can be thought of as the unwrapped surface area of the barrel-shaped detector. The vertical axis indicates the particle's transverse momentum. 

\begin{figure}
	\centering
	\begin{tikzpicture}
		\node [inner sep=0pt] (myimage) at (0,0) {\includegraphics[width=\linewidth]{Media/Pictures/Dijet_2D_Example.png}};
		\node at (-2.3,3) [align=left, fill=white] {\footnotesize ALICE Pb--Pb 2018 @ $\sqrt{s_{NN}} = 5.02\bunit{TeV}$ \\
						\footnotesize $p_T \geq 0.15\bunit{GeV}, |\eta| \leq 0.9, \chi^2_\text{track} < 300$ \\
						\footnotesize DCA$_{xy}$, DCA$_z$ $\leq 0.5\bunit{cm}$};
	\end{tikzpicture}
	\caption{A heavy ion Pb--Pb collision taken by ALICE. Besides many low-momentum thermal particles two jets can be seen. The jets consist of high-$p_T$ particles accompanied by surrounding particles with slightly lower $p_T$. Since the jets are separated by $\Delta \phi \approx \pi$ it may be that this is a correlated dijet-pair resulting from the same hard parton scattering. It is very probable, but not safe to say that they are correlated because their individual jet partners could be out of acceptance and these two are separated by $\Delta \phi \approx \pi$ by coincidence.}
	\label{fig:dijet2dexample}
\end{figure}

\subsection{Jets as trigger objects and the anti-$k_t$ algorithm}
\label{sec:antikt}

Many analyses require the presence of a jet that fulfills certain criteria, for example a minimum transverse momentum. To know the transverse momentum of a jet we need to know the total transverse momentum of the particles that belong to the jet, which is not easy to accomplish since we cannot unambiguously distinguish background particles from other objects. Another possibility is to speak about the jet as an object itself, not of its individual constituents. The mapping from a collection of single particles to a collection of jets can be done with jet clustering algorithms. FastJet \cite{cacciari_fastjet_2012} is a C++ package that provides a broad range of jet finding tools and jet analysis tools. The jet algorithm that will be used is the anti-$k_t$ sequential jet clustering algorithm, \cite{cacciari_anti-_2008} since its jet identification process is very stable under changes of underlying soft radiation. Just like in other sequential jet algorithms, two measures are introduced:

\begin{equation}
	\begin{gathered}
		d_{ij} = \min \left(k_{ti}^{-2}, k_{tj}^{-2}\right) \frac{\Delta_{ij}^2}{R^2}, \\
		d_{iB} = k_{ti}^{-2}
	\end{gathered}
	\label{eq:anti-k_t}
\end{equation}

where $\Delta_{ij}^2 = (y_i - y_j)^2 + (\phi_i - \phi_j)^2$ and $k_{ti}, y_i \text{ and } \phi_i$ are respectively the transverse momentum, rapidity and azimuth of particle $i$. The quantity $d_{iB}$ is also called the beam distance of particle $i$. The value $R$ is the jet radius parameter. Let us now look at how the algorithm uses the formulae above. The algorithm starts with the four-momenta for all particles in the event $p_i^\mu, i = 0,1,\dots, N-1$. Note that all particles, we call them protojets in this stage, are essentially light-like.\cite{ellis_successive_1993} The jet algorithm then recursively groups pairs of protojets together to form new protojets, preferably those with nearly parallel momentum so that they will eventually form part of the same jet. The way the distance measure from equation (\ref{eq:anti-k_t}) is formulated makes the clustering of soft particles with neighboring hard particles more probable than the clustering among soft particles themselves. The algorithm decides on when the joining for a particular protojet should cease, after which the protojet is called a jet and not manipulated any further.

To explain the recursive clustering of the algorithm in terms of equation (\ref{eq:anti-k_t}), there are first determined all quantities $d_{ij}$ and $d_{iB}$ for all particles $i,j$. Among these values, the smallest one is picked and labeled $d_\text{min}$. If $d_\text{min}$ is a $d_{ij}$, merge protojets $i$ and $j$ into a new protojet $k$ with

\begin{equation}
	\begin{gathered}
		p_k^\mu = p_i^\mu + p_j^\mu \\
		\text{from which directly follows}\\	
		k_{tk} = k_{ti} + k_{tj} \\
		\eta_k = \left[k_{ti} \eta_i + k_{tj} \eta_j \right] / k_{tk} \\
		\phi_k = \left[k_{ti} \phi_i + k_{tj} \phi_j \right] / k_{tk} 
	\end{gathered}
\end{equation}

so their coordinates in the phase space $(\phi, \eta)$ are weighted based on their respective transverse momenta. If $d_{min}$ is a $d_i$, the corresponding protojet $i$ is considered not mergable. It is removed from the list of protojets and added to the list of jets. This process is repeated until all protojets are in the list of jets. \cite{ellis_successive_1993} In heavy ion collisions the hard jets are always accompanied by a more diffuse background of soft particles. \cite{cacciari_fastjet_2012} Performing measurements on averages on this background makes it possible to correct the hard jets for the soft contamination, for example to isolate the summed transverse momentum of the jet from the underlying background particles contributing to this sum. It is not well defined how to distinguish background from other objects like jets. FastJet uses a technique in which first the $N$ hardest jets are removed from the (proto-)jet list and then a measurement on transverse momentum per area, so $\rho = p_T/A$ is performed over the whole event. The resulting average transverse momentum density can then be multiplied with the jet area of jet $i$ to get the contribution of the soft particle background to jet $i$ and subtracted if desired to get the non-background contribution of the jet constituents. 

\subsection{Hadrons as trigger objects}

Identifying high-$p_T$ objects by clustering particles to jets as described in section \ref{sec:antikt} can sometimes lead to unwanted bias effects. The behavior of clustering algorithms is not invariant under changes of local measures, for example the local particle density in the $(\phi, \eta)$ phase space or the local particle transverse momentum spectrum. This can lead to biases if one conducts analyses with different starting conditions. As an example, let us consider we would like to trigger for a jet that has a transverse momentum of more than $30\bunit{GeV}$. Since the total transverse momentum of the jet is the sum of the transverse momenta of its constituents, more constituents that are available for clustering means that this threshold is reached quicker. For a positive elliptic flow parameter $v_2$, one will find more particles in the event plane that can be used for jet clustering than out of the event plane. As a direct consequence, jets are preferably clustered in-plane than elsewhere although the average initial distribution of hard hadrons is uniform in azimuthal angle. The jet's azimuthal position is therefore biased by the elliptic flow of low-momentum particles.

To overcome this issue it is also possible to take single, high-$p_T$ hadrons as trigger objects. They occur independently from the anisotropic flow of low-$p_T$ hadrons and are therefore not biased. Usually the threshold for the hadron's transverse momentum can be lowered compared to the jet's transverse momentum threshold because the leading hadron of the jet as one constituent of many only contributes a fraction of its total $p_T$. There is an issue of this hadron trigger method when triggering for a dihadron pair. When high-$p_T$ hadrons are scattered across the $(\phi, \eta)$ phase space and it happens that the two hadrons with the highest transverse momenta are right next to each other, for instance if they belong to the same jet, the subleading hadron has to be identified to originate from the same jet as the leading hadron. Since in the dijet/dihadron analysis the leading/subleading trigger are defined as the ones with the highest/second-highest transverse momentum, the two lying next to each other would be identified as the leading/subleading triggers. But this would consequently lead to the event being omitted, because their separation in azimuthal angle is too small (in the dijet/dihadron analysis we require the two triggers to be separated by $|\Delta \phi_{\text{jet}_1 , \text{jet}_2}| > \pi/2$).  \cite{yang_background-free_2025} To do so, we first sort the $N$ high-$p_T$ hadrons (above a certain threshold, e.g. $5\bunit{GeV}$) by their transverse momenta to have 

\begin{equation}
	p_{T\text{,high}}^{(0)} \geq p_{T\text{,high}}^{(1)} \geq \dots \geq p_{T\text{,high}}^{(N-1)} \, .
\end{equation} 

In each iteration we pick the particle with the highest particle with $p_{T\text{,high}}^{(0)}$ and erase all other high-$p_T$ hadrons in a radius $R$, because they should belong to the same jet. The value of $R$ could be chosen to be the same as a jet radius in an analysis where jets are used as trigger objects. We do this until all high-$p_T$ particles stand alone within a radius of $R$.

Both methods, using hadrons as trigger objects as well as using jets, will be used in the analysis.

\end{document}